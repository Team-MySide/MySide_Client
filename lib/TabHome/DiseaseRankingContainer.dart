import 'package:flutter/material.dart';

class DiseaseRankingContainer extends StatelessWidget {
  const DiseaseRankingContainer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Test"),
    );
  }
}
